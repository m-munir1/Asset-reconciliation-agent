# Optional. Get a free key at https://aistudio.google.com/apikey
# The agent runs fully without this — it only enables the LLM-written
# reviewer summary instead of the template-based one.
GEMINI_API_KEY=
